#pragma once

#include <cstddef>

namespace jmnel::generic {

    template <typename T, size_t extentT, size_t strideT = 1>
    class Span {
    private:
    public:
    };

}  // namespace jmnel::generic
