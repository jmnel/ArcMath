#pragma once

#include "GenericPointerIterator.hpp"
#include "Traits.hpp"

namespace jmnel::matrix::detail {

    template <typename Derived>
    class IteratorAccess {
    private:
        using Scalar = traits<Derived>::Scalar;

    public:
        jmnel::generics::GenericPointerIterator <
    };

}  // namespace jmnel::matrix::detail
