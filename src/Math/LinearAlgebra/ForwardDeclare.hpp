#pragma once

#include <cstddef>

#include "Common.hpp"

namespace jmnel::matrix {

    template <typename ScalarT, size_t rowsT, size_t colsT, typename OptionsT>
    class Matrix;

    namespace detail {}

}  // namespace jmnel::matrix
