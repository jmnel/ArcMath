#include <GUI/Widgets/TiledLayoutView.hpp>

namespace arc::gui {

    // -- Constructor --
    TileLayoutView::TileLayoutView( QWidget* parent ) noexcept
        : QWidget( parent ) {
    }

    // -- Destructor --
    TileLayoutView::~TileLayoutView() noexcept {
    }

}  // namespace arc::gui
